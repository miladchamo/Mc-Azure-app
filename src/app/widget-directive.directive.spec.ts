import { WidgetDirectiveDirective } from './widget-directive.directive';

describe('WidgetDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
